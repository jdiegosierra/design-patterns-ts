
/*Software entities should be open for extension but closed for modification. Si se nos da el caso en el que una clase
tiene que actuar diferente en función del tipo, Se puede crear una clase abtracta y crear las distintas subclases que
sobreescriban el método base. También se puede hascer uno una interfaz perpo es mejor utilizarla para desacoplar entre capas.
Si por el hecho de ejecutar una ación, despuén van más acciones sucesivas, entonces lo mejor es utilizar comunicación por eventos.
Y enviar a la cola que un video ha sido guardado para que los demas servicios hagan sus cosas
    */

class CreditCard {
    constructor() {
    }
}


(function main(){

})();
